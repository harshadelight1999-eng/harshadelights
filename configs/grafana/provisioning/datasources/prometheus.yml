# Grafana Datasource Configuration
# Automatically configures Prometheus as the primary datasource

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      httpMethod: POST
      manageAlerts: true
      prometheusType: Prometheus
      prometheusVersion: 2.40.0
      cacheLevel: 'High'
      disableMetricsLookup: false
      customQueryParameters: ''
      timeInterval: '30s'
    secureJsonData: {}
    version: 1

  # Additional datasource for application logs (if using Loki)
  # - name: Loki
  #   type: loki
  #   access: proxy
  #   url: http://loki:3100
  #   isDefault: false
  #   editable: true
  #   jsonData:
  #     maxLines: 1000
  #     derivedFields:
  #       - datasourceUid: prometheus
  #         matcherRegex: "traceID=(\\w+)"
  #         name: TraceID
  #         url: "$${__value.raw}"
  #   version: 1